
name: web workflow

on:
    push

jobs:
    test-job:
        runs-on: ubuntu-latest

        steps:
            - name: Display name
              run: echo " Hi Praveen..."

            - name: Display the date
              run: date  

              



